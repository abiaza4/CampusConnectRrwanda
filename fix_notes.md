# Fix Notes

## Logo Issue
The logo image has a dark background (not transparent), so it appears as a small dark square in the navbar. Need to regenerate a logo with transparent background, or use an SVG icon approach instead.

## Remaining Fixes
1. **Logo**: The generated logo has a dark background - need to either regenerate with transparent bg or use an SVG/CSS approach
2. **Navbar scrolled state**: The "Cost Calculator" link text doesn't show full label on mobile - just "Cost"
3. **Stats counters**: Fixed with initial value = end, animate on scroll
4. **University profile not found**: Fixed with branded fallback
5. **Search wiring**: Hero search now navigates to /universities with query param
6. **Testimonials removed**: Replaced with platform features (content policy)
7. **NotFound page**: Branded to match site theme
